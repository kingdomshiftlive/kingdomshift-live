org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Note: 16 KB page size support is automatically handled by Flutter and modern plugins
# The Android Gradle Plugin 8.1+ automatically validates native libraries for 16 KB page size compatibility
# No additional configuration needed - the build system will validate during compilation

# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
